# Path => /etc/sysconfig/network        
# Effect => 用于指定本机的网络配置信息

# The Following is Content Template
# Format => <key(大写)>=<value>
NETWORKING=yes                          # yes 开启 ipv4 网络，no 关闭 ipv4 网络
NETWORKING_IPV6=yes                     # yes 开启 ipv6 网络，no 关闭 ipv6 网络
HOSTNAME=Haku                           # 主机名，必须和 /etc/hostname 文件中的内容一样
FORWARD_IPV4=yes/no　　　　             # 是否开启IP转发功能 
GATEWAY=192.168.1.1                     # 配置默认网关 ip 地址，最低优先级的网关配置，其他地方配了就以其他地方配置的网关为准
GAREWAYDEV=gw-dev　　                   # gw-dw表示网关的设备名，如：etho等 
PEERDNS=no                              # 表示不自动修改 /etc/resolv.conf 文件的内容
